// Minimal legacy stub. Real activation logic is in extension-main.js (see package.json main).
// Kept only for backward compatibility with any stale imports.
function activate() {}
function deactivate() {}
module.exports = { activate, deactivate };