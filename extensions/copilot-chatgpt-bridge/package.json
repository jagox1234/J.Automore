{
  "name": "copilot-chatgpt-bridge",
  "displayName": "Copilot + ChatGPT Bridge",
  "description": "Puente interactivo entre GitHub Copilot y ChatGPT (OpenAI) con objetivo guiado.",
  "version": "0.0.1",
  "publisher": "your-name",
  "engines": { "vscode": "^1.90.0" },
  "categories": ["Other"],
  "activationEvents": [
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      { "command": "copilotBridge.openChatPanel", "title": "Copilot Bridge: Open Panel" },
      { "command": "copilotBridge.reindex", "title": "Copilot Bridge: Reindex Project" },
      { "command": "copilotBridge.summarizeMemory", "title": "Copilot Bridge: Summarize Memory Now" },
      { "command": "copilotBridge.showMetrics", "title": "Copilot Bridge: Show Metrics" }
    ],
    "configuration": {
      "title": "Copilot Bridge",
      "properties": {
        "copilotBridge.openaiApiKey": {
          "type": "string",
            "default": "",
            "description": "OpenAI API Key para ChatGPT (almacenada en settings.json)"
        },
        "copilotBridge.model": {
          "type": "string",
          "default": "gpt-4o-mini",
          "description": "Modelo OpenAI a usar"
        },
        "copilotBridge.insertMode": {
          "type": "string",
          "enum": ["comment", "inline"],
          "default": "comment",
          "description": "Cómo insertar respuestas: comentario o inline"
        }
      }
    }
  },
  "scripts": {
    "build": "esbuild src/extension.ts --bundle --platform=node --external:vscode --format=cjs --outfile=dist/extension.js",
    "watch": "npm run build -- --watch",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/vscode": "^1.90.0",
    "@types/node": "^20.11.30",
    "esbuild": "^0.21.0",
    "typescript": "^5.5.0",
    "vsce": "^2.15.0"
  },
  "dependencies": {
    "openai": "^4.0.0"
  }
}
